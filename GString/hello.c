#include<glib.h>
#include<gmodule.h>

GString* s = g_string_new (const gchar *init =  NULL);
char* val ="This is string";

g_string_append (GString *s,
                 const gchar *val);
printf("SS:'%s'\n", g_string_printf (GString *s,
                 const gchar *c));
return 0;
}

